org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.nonTransitiveRClass=true
# The app uses only the Android framework — no AndroidX, no third-party
# libraries — so there is nothing to jetify and no dependency graph to resolve.
android.useAndroidX=false
